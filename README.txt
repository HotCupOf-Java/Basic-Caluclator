#Basic Calculator / Python

This is a simple calculator that takes two **integer** inputs, and displays the answer on the console depending on the selection operator. 
That's it. Nothing special, just a little program that does work. 